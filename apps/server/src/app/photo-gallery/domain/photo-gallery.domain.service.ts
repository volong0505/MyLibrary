import { PhotoGallery } from "./photo-gallery";

export class RemittanceOptions {
    readonly photo: PhotoGallery;
    readonly receiver: PhotoGallery;
    readonly amount: number;
}

export class PhotoGalleryDomainService { }
